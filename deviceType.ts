/**
 * @author Kalender Hisir
 * @desc Returns the device type (enum)
 * @returns mobile|tablet|desktop
 */

export enum DeviceType {
  Mobile = 'mobile',
  Tablet = 'tablet',
  Desktop = 'desktop'
}
